# 多周期回测处理机制说明（修订版）

## 你的场景

**买入条件：**
- 均线多头排列（data_interval=日线）
- 回踩MA20（data_interval=日线）

**卖出条件：**
- 缩量阴线（data_interval=5分钟）

**UI参数设置：**
- **K线周期：日线**（这是execution_interval，决定主循环步长）
- **K线数量：3000根**

## 核心概念澄清

### K线周期（execution_interval）的含义

**K线周期=日线** 不是指"只加载日线数据"，而是指：

1. **主回测循环按日线推进**
   - 逐根遍历3000根日线K线
   - 每根日线作为一个回测时间点

2. **在每个时间点，根据条件的 data_interval 动态加载相应周期数据**
   - 买入条件的 data_interval=日线 → 使用日线数据评估
   - 卖出条件的 data_interval=5分钟 → 加载该日期的5分钟数据评估

## 回测处理流程

### 1. 数据需求分析（analyze_data_requirements）

程序分析策略需要哪些周期的数据：

```python
# 在 scan_engine.py 第249行
req = analyze_data_requirements(strategy.buy_tree, execution_interval)
is_multi_timeframe = len(req.intervals) > 1

# 对于你的策略，会检测到：
# req.intervals = [Interval.DAILY, Interval.MINUTE_5]
# is_multi_timeframe = True  # 多周期策略
```

### 2. 主循环数据加载（按 execution_interval）

```python
# 加载3000根日线作为主循环数据
all_bars = load_bars(symbol, Interval.DAILY, 3000)

# 逐根遍历日线
for i in range(len(all_bars)):
    current_daily_bar = all_bars[i]
    current_date = current_daily_bar.datetime.date()
```

### 3. 买入条件评估（使用日线数据）

在每根日线K线上：

```python
# 构造多周期上下文
eval_time = current_daily_bar.datetime
ctx = MultiTimeframeContext(symbol=symbol, evaluation_time=eval_time)

# 为买入条件加载日线数据（data_interval=DAILY）
ctx.set_bars(Interval.DAILY, all_bars[:i+1])

# 评估买入条件（均线多头排列、回踩MA20）
passed = strategy.buy_tree.evaluate(symbol, ctx)
```

### 4. 卖出条件评估（加载5分钟数据）

**关键点**：当买入条件满足、进入持仓后，需要评估卖出条件（缩量阴线）

```python
if passed:  # 买入条件满足
    # 进入持仓，开始检查卖出条件
    entry_date = current_date
    
    # 从进入日开始，逐日检查卖出条件
    for j in range(i+1, len(all_bars)):
        holding_date = all_bars[j].datetime.date()
        
        # === 关键：加载 holding_date 这一天的5分钟数据 ===
        minute_5_bars = load_minute_bars_for_date(
            symbol, 
            Interval.MINUTE_5, 
            holding_date
        )
        
        # 构造多周期上下文，包含5分钟数据
        ctx_sell = MultiTimeframeContext(...)
        ctx_sell.set_bars(Interval.MINUTE_5, minute_5_bars)
        
        # 评估卖出条件（缩量阴线，data_interval=5分钟）
        sell_triggered = strategy.sell_tree.evaluate(symbol, ctx_sell)
        
        if sell_triggered:
            exit_date = holding_date
            break
```

### 5. 实际实现机制

程序通过 `_get_bars_as_of()` 和 `MTFCandleBuffer` 实现：

```python
# 在 scan_engine.py 第675行
def _get_bars_as_of(self, symbol: str, n: int, interval: Interval,
                    as_of_time) -> list:
    """
    获取截至指定时间点的K线（防止未来函数）
    """
    if self._mtf_buffer is not None:
        # 使用 MTFCandleBuffer，支持多周期时间对齐
        bars = self._mtf_buffer.get_bars_as_of(
            symbol, n, interval, as_of_time
        )
        return bars
    
    # 回退到传统模式
    return self._get_bars(symbol, n, interval)
```

**MTFCandleBuffer 的作用**：
- 维护多个周期的K线数据（日线 + 5分钟）
- `get_bars_as_of(symbol, n, Interval.MINUTE_5, as_of_time)` 会返回：
  - 截至 `as_of_time` 的最近 n 根5分钟K线
  - 确保不会使用未来数据

### 6. 时间对齐示例

假设回测到 2020-01-15（日线K线）：

**买入条件评估**：
```python
# 使用日线数据
daily_bars = [..., 2020-01-13, 2020-01-14, 2020-01-15]
# 评估：均线多头排列、回踩MA20 → 通过
```

**进入持仓后，在 2020-01-16 评估卖出条件**：
```python
# 加载 2020-01-16 这一天的所有5分钟K线
minute_5_bars_20200116 = [
    2020-01-16 09:35,
    2020-01-16 09:40,
    2020-01-16 09:45,
    ...
    2020-01-16 14:55,
    2020-01-16 15:00
]
# 评估：缩量阴线（data_interval=5分钟）
```

**在 2020-01-17 继续评估卖出条件**：
```python
# 加载 2020-01-17 这一天的所有5分钟K线
minute_5_bars_20200117 = [
    2020-01-17 09:35,
    2020-01-17 09:40,
    ...
]
# 评估：缩量阴线 → 触发卖出
```

## 数据加载策略

### execution_interval = 日线

1. **主数据加载**：
   - 加载3000根日线K线
   - 这是回测的主时间轴

2. **按需加载辅助周期数据**：
   - 当条件的 `data_interval != execution_interval` 时
   - 动态加载该周期的数据
   - 对于你的策略：持仓期间，每天加载当天的5分钟数据

### 数据对齐（防未来函数）

```python
# MTFCandleBuffer 确保时间对齐
bars = mtf_buffer.get_bars_as_of(
    symbol="600519.SSE",
    n=240,  # 最近240根5分钟K线（一天）
    interval=Interval.MINUTE_5,
    as_of_time=datetime(2020, 1, 16, 15, 0)  # 当天收盘时间
)
# 返回：只包含 datetime <= 2020-01-16 15:00 的5分钟K线
```

## 关键要点

### ✅ 正确理解

1. **K线周期=日线** ≠ 只用日线数据
   - 主循环按日线推进
   - 条件评估时根据 data_interval 加载相应周期数据

2. **多周期数据按需加载**
   - 买入条件评估：使用日线数据
   - 卖出条件评估：加载5分钟数据
   - 通过 `MTFCandleBuffer` 自动管理

3. **时间对齐**
   - 每个时间点，所有周期的数据都只包含该时间点之前的数据
   - 防止未来函数

### ⚠️ 注意事项

1. **数据库要有完整的5分钟数据**
   - 回测3000根日线，时间跨度约12年
   - 需要这12年内所有交易日的5分钟数据
   - 如果5分钟数据缺失，卖出条件评估会失败

2. **MTFCandleBuffer 必须正确初始化**
   - UI在调用回测前，必须创建 `MTFCandleBuffer`
   - 加载所有需要的周期数据（日线 + 5分钟）
   - 传给 `ScanEngine`

3. **性能考虑**
   - 多周期回测比单周期慢
   - 需要频繁加载5分钟数据
   - 使用缓存机制优化

## 回测流程总结

```
1. 加载3000根日线K线（主时间轴）
   ↓
2. 遍历每根日线：
   ├─ 构造多周期上下文
   ├─ 加载日线数据（买入条件 data_interval=日线）
   ├─ 评估买入条件（均线多头排列、回踩MA20）
   │
   └─ 如果买入条件满足：
       ├─ 进入持仓
       ├─ 逐日检查卖出条件：
       │   ├─ 加载当日5分钟K线（卖出条件 data_interval=5分钟）
       │   ├─ 构造多周期上下文
       │   ├─ 评估卖出条件（缩量阴线）
       │   └─ 如果触发，记录卖出，跳出持仓循环
       │
       └─ 继续主循环
```

## 推荐配置

对于你的策略（日线买入 + 5分钟卖出）：

```
K线周期（execution_interval）：日线  ← 主循环步长
K线数量：3000根
回测时间范围：2020-01-01 ~ 今日

条件配置：
  买入条件：
    ├─ 均线多头排列 (data_interval=日线)
    └─ 回踩MA20 (data_interval=日线)
  
  卖出条件：
    └─ 缩量阴线 (data_interval=5分钟)
```

这样配置，程序会：
1. 按日线逐根遍历3000根K线
2. 用日线数据评估买入条件
3. 持仓期间，每天加载当天的5分钟数据评估卖出条件
4. 确保所有数据时间对齐，防止未来函数