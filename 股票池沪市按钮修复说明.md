#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""股票池沪市按钮问题 - 完整修复方案"""

import sys
from pathlib import Path

# 添加项目路径
project_root = Path(__file__).parent
sys.path.insert(0, str(project_root))

print("="*60)
print("股票池沪市按钮修复方案")
print("="*60)

print("\n问题诊断结果：")
print("  缓存中的股票数量为 0，这导致点击沪市按钮时无数据显示。")

print("\n" + "="*60)
print("解决方案（请按顺序执行）：")
print("="*60)

print("\n方案1：重新加载缓存（推荐）")
print("  步骤：")
print("  1. 关闭当前运行的应用程序")
print("  2. 运行预加载脚本：")
print("     python _preload_stock_pool.py")
print("  3. 等待加载完成后，重新启动应用程序")
print("  4. 再次点击沪市按钮测试")

print("\n方案2：如果方案1不work，手动初始化缓存")
print("  运行以下代码：")
print("  python -c "from vnpy.trader.stock_pool import force_refresh_cache; force_refresh_cache()"")

print("\n方案3：临时解决（手动输入）")
print("  参考文档：临时解决方案_手动输入股票池.md")
print("  直接在股票池文本框中输入股票代码")

print("\n" + "="*60)
print("技术说明：")
print("="*60)
print("问题原因：")
print("  股票池采用异步加载机制，首次点击时缓存未初始化，")
print("  虽然有1.5秒后的自动重试，但如果后台加载未完成，")
print("  重试仍会返回空列表。")
print("\n已实现的修复（在代码中）：")
print("  1. widget.py: _set_exchange_pool方法有重试机制")
print("  2. stock_pool.py: 使用缓存管理避免重复查询数据库")
print("\n但需要确保缓存已初始化才能正常工作。")
print("="*60)
