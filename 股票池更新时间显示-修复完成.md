# 股票池更新时间显示功能 - 修复完成

## 任务目标
在"创业板-1402"右侧显示股票池数据的最近更新时间

## 问题诊断

### 1. 发现的问题
- `vnpy/trader/stock_pool.py` 中的 `get_pool_update_time()` 函数调用了不存在的 `_get_cache_path()` 函数
- 导致无法正确读取缓存文件中的更新时间

### 2. 根本原因
```python
# 错误的代码：
def get_pool_update_time() -> str:
    """获取股票池数据的更新时间（用于UI显示）"""
    try:
        cache_path = _get_cache_path()  # ❌ _get_cache_path() 函数不存在
        if cache_path.exists():
            import json
            with open(cache_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
                return data.get('update_time', '')
    except Exception:
        pass
    return ""
```

## 修复方案

### 修改内容
将 `_get_cache_path()` 替换为已定义的 `_SYMBOLS_DISK_CACHE` 常量：

```python
# 修复后的代码：
def get_pool_update_time() -> str:
    """获取股票池数据的更新时间（用于UI显示）"""
    try:
        cache_path = _SYMBOLS_DISK_CACHE  # ✅ 使用已定义的缓存路径常量
        if cache_path.exists():
            import json
            with open(cache_path, 'r', encoding='utf-8') as f:
                data = json.load(f)
                return data.get('update_time', '')
    except Exception:
        pass
    return ""
```

### 修复命令
```bash
python -c "import re; f=open('vnpy/trader/stock_pool.py','r',encoding='utf-8'); c=f.read(); f.close(); c=re.sub(r'cache_path = _get_cache_path\(\)', r'cache_path = _SYMBOLS_DISK_CACHE', c); f=open('vnpy/trader/stock_pool.py','w',encoding='utf-8'); f.write(c); f.close(); print('修复完成')"
```

## 测试验证

### 测试结果
```
[SUCCESS] 成功获取股票池更新时间: 2026-08-15 10:19

缓存文件路径: C:\Users\11229\.vnpy\stock_pool\symbols_cache.json
文件存在: True
缓存文件中的 update_time: 2026-08-15 10:19:08
缓存的股票数量: SSE=0, SZSE=0
```

✅ **函数现在可以正常工作，成功读取到更新时间！**

## UI显示效果

根据之前的代码分析，更新时间会显示在股票池数量标签的右侧：

```python
# 在 vnpy/quant_research/ui/behavior_tab.py 中
update_time = get_pool_update_time()
if update_time:
    pool_label.setText(f"{exchange_name} - {count}只  (更新: {update_time})")
else:
    pool_label.setText(f"{exchange_name} - {count}只")
```

### 显示示例
- **之前**: "创业板 - 1402只"
- **现在**: "创业板 - 1402只  (更新: 2026-08-15 10:19)"

## 相关文件

### 修改的文件
- `vnpy/trader/stock_pool.py` - 修复了 `get_pool_update_time()` 函数

### UI使用位置
- `vnpy/quant_research/ui/behavior_tab.py` - 在股票池标签中显示更新时间

### 数据来源
- `~/.vnpy/stock_pool/symbols_cache.json` - 股票池缓存文件，包含 `update_time` 字段

## 总结

✅ **修复完成！** `get_pool_update_time()` 函数现在可以正确读取缓存文件中的更新时间，UI界面会在股票池数量标签右侧显示最近的更新时间。

用户只需重启应用，即可看到"创业板-1402只"旁边显示更新时间。
