# 多周期策略回测问题 - 操作指南

## 问题现状

你的策略设置了：
- 买入条件：均线多头排列(日线) + 回踩MA20(日线) + 缩量阴线(5分钟)
- 界面显示"数据周期 = 5分钟"

但回测结果持仓天数是54天、105天等**整数天**，说明回测引擎仍在按**日线遍历**，而不是5分钟。

## 根本原因

界面上的"数据周期"下拉框**只是显示，没有保存到条件对象**：

1. `Condition` 类有 `data_interval` 属性
2. 但 `widget.py` 的参数面板中，下拉框的值**没有写入** `condition.data_interval`
3. 所以回测时 `mtf_auto_loader.py` 检测不到5分钟周期
4. `execution_interval` 默认回退到 `anchor_interval`（日线）

## 临时解决方案（手动测试）

现在你需要**手动运行一个测试脚本**来验证多周期回测能否正确工作。我创建一个脚本：

```python
# _test_mtf_manual.py
import sys
sys.path.insert(0, 'c:/Users/11229/Documents/GitHub/vnpy')

from vnpy.trader.constant import Interval
from vnpy.strategy_condition.core.condition import Condition
from vnpy.strategy_condition.core.condition_tree import ConditionNode, ConditionTree
from vnpy.strategy_condition.constant import ConditionCategory, ConditionIndicator, NodeOp
from vnpy.strategy_condition.core.strategy import Strategy

# 手动创建条件并设置 data_interval
ma_cond = Condition(
    category=ConditionCategory.TREND,
    indicator=ConditionIndicator.MA_ALIGNMENT,
    params={"periods": [5, 10, 20, 60], "max_gap_pct": 0.0},
    data_interval=Interval.DAILY  # 手动设置日线
)

vol_cond = Condition(
    category=ConditionCategory.VOLUME,
    indicator=ConditionIndicator.VOLUME_SHRINK,
    params={"vol_ratio": 0.8, "period": 5},
    data_interval=Interval.MINUTE_5  # 手动设置5分钟
)

# 构建条件树
buy_tree = ConditionTree(
    op=NodeOp.AND,
    children=[
        ConditionNode(op=NodeOp.LEAF, condition=ma_cond),
        ConditionNode(op=NodeOp.LEAF, condition=vol_cond)
    ]
)

strategy = Strategy(name="手动测试MTF", buy_tree=buy_tree, sell_tree=None)

# 分析数据需求
from vnpy.strategy_condition.core.mtf_auto_loader import analyze_strategy_data_requirements
req = analyze_strategy_data_requirements(strategy, Interval.DAILY, 100)

print("="*60)
print("多周期策略数据需求分析")
print("="*60)
print(f"所需周期: {[i.value for i in req.required_intervals]}")
print(f"锚点周期: {req.anchor_interval.value}")
print(f"执行周期: {req.execution_interval.value}")
print()
if req.execution_interval == Interval.MINUTE_5:
    print("✓ 正确：回测将按5分钟遍历")
else:
    print("✗ 错误：回测仍按日线遍历")
```

运行这个脚本：
```bash
cd c:\Users\11229\Documents\GitHub\vnpy
python _test_mtf_manual.py
```

如果输出显示"✓ 正确"，说明底层逻辑没问题，**只需要修复UI层**。

## 长期解决方案（修复UI）

需要修改 `vnpy/strategy_condition/ui/widget.py` 中参数面板的逻辑：

### 修改位置

在 `widget.py` 中找到处理"数据周期"下拉框的代码（大约在参数面板的创建和保存部分），需要：

1. **加载时**：从 `condition.data_interval` 读取值并设置到下拉框
2. **保存时**：从下拉框读取值并写入 `condition.data_interval`

### 示例伪代码

```python
# 加载参数时
if hasattr(condition, 'data_interval') and condition.data_interval:
    interval_combo.setCurrentText(condition.data_interval.value)

# 保存参数时  
selected_interval_text = interval_combo.currentText()
if selected_interval_text and selected_interval_text != "日线":
    condition.data_interval = Interval(selected_interval_text)
else:
    condition.data_interval = Interval.DAILY
```

## 你现在要做什么

1. **先运行上面的测试脚本**确认底层逻辑正确
2. 如果底层正确，**重新创建策略**：
   - 删除现有策略
   - 重新添加条件
   - 确保"数据周期"下拉框选择5分钟
   - 点保存
3. 回测前，先导出策略JSON查看 `data_interval` 字段是否存在
4. 如果还是不行，需要修复 `widget.py` 的参数面板保存逻辑

## 回测应该的正确行为

当你的策略正确保存后，回测应该：

1. **主循环** = 每根5分钟K线
2. **日线条件**（均线多头、回踩MA20）= 用5分钟close实时合成虚拟日线评估
3. **5分钟条件**（缩量阴线）= 直接用当前5分钟K线评估
4. **触发时机** = 某根5分钟K线同时满足所有条件时立即买入

持仓时间将变成**分钟级**（如持仓325分钟 = 5小时25分），而不是整数天。