# 股票池选择卡顿 - 立即解决方案

## 🚨 问题描述
选择指数、行业、板块时，程序卡死10-60秒，界面完全无响应。

## ✅ 立即解决（3步）

### 1. 关闭所有VeighNa程序
确保没有任何VeighNa程序在运行，避免数据库被占用。

### 2. 运行预热脚本
在命令行中执行：
```cmd
python _preload_stock_pool.py
```

### 3. 等待完成
- 脚本会显示进度（约10-60秒）
- 完成后会显示缓存的股票数量
- 看到"✅ 缓存预热完成"后，就可以启动程序了

## 📊 效果对比

| 操作 | 优化前 | 优化后 |
|------|--------|--------|
| 首次选择股票池 | 卡死10-60秒 | 立即响应（<0.1秒） |
| 后续选择 | 每次都卡 | 秒开 |
| 缓存有效期 | 无 | 1小时 |

## 🔧 工作原理

**为什么会卡？**
- 选择股票池时需要查询101GB的数据库
- 查询 `bar_overview` 表获取所有股票列表
- 在主UI线程执行，导致界面卡死

**预热脚本做了什么？**
1. 提前查询数据库，加载所有股票信息
2. 将结果缓存到本地JSON文件
3. 之后程序直接读取缓存，秒开

**缓存位置：**
```
C:\Users\11229\.vnpy\stock_pool\symbols_cache.json
```

## 🔄 长期使用建议

### 方案A：每天运行一次预热脚本
创建批处理文件 `预热股票池.bat`：
```cmd
@echo off
cd /d %~dp0
python _preload_stock_pool.py
pause
```
每天开盘前双击运行一次。

### 方案B：迁移数据库到SSD
数据库在C盘（SSD）比在机械硬盘快10倍以上。
参考：`数据库迁移操作说明.md`

### 方案C：创建数据库索引
在数据库中添加索引加速查询：
```sql
CREATE INDEX IF NOT EXISTS idx_bar_overview_interval 
ON dbbardata(interval);
```

## 📝 运行选股 vs 回测验证

### 运行选股（扫描）
- **目的**：从股票池中找出符合买入条件的股票
- **过程**：
  1. 读取股票池列表
  2. 逐个股票检查是否满足买入条件
  3. 输出符合条件的股票信号
- **结果**：推荐买入的股票列表
- **不涉及**：卖出判断、盈亏计算

### 回测验证（完整模拟）
- **目的**：验证策略的实际盈利能力
- **过程**：
  1. 读取股票池列表
  2. 模拟买入（满足条件时）
  3. 模拟持仓（每天检查卖出条件）
  4. 模拟卖出（满足条件或止损）
  5. 计算盈亏统计
- **结果**：完整的交易记录（买入价、卖出价、收益率）
- **包含**：完整的交易生命周期

**关键区别：**
- 运行选股只看"现在"，回测看"历史全过程"
- 运行选股快（只检查最新数据），回测慢（需要遍历历史）
- 运行选股给信号，回测给收益评估

## ⚠️ 注意事项

1. **缓存有效期**：1小时后过期，需要重新查询数据库
2. **数据更新**：如果导入了新数据，需要重新预热缓存
3. **磁盘空间**：缓存文件约几百KB，可以忽略不计
4. **线程安全**：程序运行时不要同时运行预热脚本

## 🆘 如果预热失败

### 错误1：数据库被占用
```
database is locked
```
**解决**：关闭所有VeighNa程序，重新运行预热脚本

### 错误2：找不到数据库
```
❌ 数据库不存在
```
**解决**：检查数据库路径是否正确，修改脚本中的 `db_path`

### 错误3：内存不足
```
MemoryError
```
**解决**：关闭其他程序释放内存，或者重启电脑后再试

## 📞 技术支持

如果遇到其他问题，请提供：
1. 预热脚本的完整输出
2. 数据库大小和位置
3. 系统内存大小
4. 错误截图
