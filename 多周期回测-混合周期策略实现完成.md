# 多周期回测：混合周期策略实现完成

## 问题背景

用户需要实现一个混合周期策略：
- **买入条件**：均线多头排列、回踩MA20（**日线**数据周期）
- **买入条件**：缩量阴线（**5分钟**数据周期）

**核心需求**：
1. 主循环按**5分钟K线**遍历
2. 日线条件用**真实历史日线** + **虚拟当天日线**（当天日线的close用当前5分钟close代替）
3. 分钟线条件直接用5分钟数据

## 实现原理

### 回测主循环（scan_engine.py）

```python
for i, minute_bar in enumerate(minute_bars):  # 遍历5分钟K线
    # 1. 评估日线条件
    if 日线条件:
        # 构造实时日线序列
        realtime_daily = [
            ...历史完整日线(截至昨天)...,
            虚拟当天日线(close=minute_bar.close)  # 关键！
        ]
        # 用这个序列计算MA5/MA10/MA20，判断多头排列和回踩
    
    # 2. 评估分钟线条件
    if 分钟线条件:
        # 直接用5分钟K线序列判断缩量阴线
    
    # 3. 两者都满足 → 触发信号
```

### 关键实现：`_build_realtime_daily_bars()`

**位置**：`vnpy/strategy_condition/engine/scan_engine.py` line ~420

```python
def _build_realtime_daily_bars(i: int) -> List[BarData]:
    """
    构造实时日线序列（历史完整日线 + 虚拟当天日线）
    
    Args:
        i: 当前分钟K线索引
    
    Returns:
        [日线1, 日线2, ..., 日线N-1, 虚拟今日K线]
    """
    cur_minute_bar = all_bars[i]
    cur_date = cur_minute_bar.dt.date()
    
    # 1. 获取历史完整日线（截至昨天）
    historical_daily = mtf_buffer.get(symbol, 0, Interval.DAILY)
    cutoff_idx = len([b for b in historical_daily if b.dt.date() < cur_date])
    
    # 2. 构造虚拟当天日线
    virtual_bar = BarData(
        symbol=symbol,
        datetime=cur_date,
        open=...,   # 从历史推断或使用今天首根分钟open
        high=...,   # 当前时刻的max(所有今天分钟high)
        low=...,    # 当前时刻的min(所有今天分钟low)
        close=cur_minute_bar.close,  # 🔑 关键：用当前分钟close
        volume=...  # 今天累计volume
    )
    
    # 3. 返回组合
    return historical_daily[:cutoff_idx] + [virtual_bar]
```

### UI层修改：主循环数据切换

**位置**：`vnpy/strategy_condition/ui/widget.py` line ~1533

```python
def _on_backtest(self):
    # ... 前面加载日线数据 ...
    
    # 分析策略需要的周期
    req = analyze_strategy_data_requirements(strategy, anchor_interval, n_bars)
    execution_interval = req.execution_interval  # 5分钟
    
    # ═══ 核心逻辑 ═══
    if execution_interval != anchor_interval and 多周期:
        # 替换bars_dict为分钟数据（主循环需要）
        exec_bars_dict = {}
        for symbol in loaded:
            minute_bars = load_bars_by_date_range(
                symbol, execution_interval, start_date, end_date
            )
            exec_bars_dict[symbol] = minute_bars
        
        bars_dict = exec_bars_dict  # 主循环遍历分钟线
        # 日线数据已在MTF Buffer中，供日线条件查询
    
    # 传入分钟数据作为主循环
    se.backtest(loaded, strategy, bars_dict, ...)
```

## 数据流程图

```
用户界面(widget.py)
    │
    ├─ 加载日线数据 → MTF Buffer (日线条件查询用)
    │
    ├─ 加载5分钟数据 → bars_dict (主循环遍历)
    │
    ↓
scan_engine.backtest()
    │
    ├─ for minute_bar in bars_dict[symbol]:  # 遍历5分钟
    │     │
    │     ├─ 评估日线条件:
    │     │    realtime_daily = _build_realtime_daily_bars(i)
    │     │    # → [历史日线..., 虚拟今日(close=minute_bar.close)]
    │     │    MA20 = calculate_ma(realtime_daily, 20)
    │     │    check_pullback(MA20)
    │     │
    │     ├─ 评估分钟条件:
    │     │    check_shrinking_volume(minute_bars)
    │     │
    │     └─ 两者都满足 → 买入信号
    │
    └─ 返回回测结果
```

## 完整示例

### 策略定义

```python
from vnpy.trader.constant import Interval

strategy = ConditionStrategy(
    name="日线回踩+分钟缩量",
    buy_tree=AND(
        # 日线条件
        均线多头排列(data_interval=Interval.DAILY),
        回踩MA20(data_interval=Interval.DAILY),
        # 分钟条件
        缩量阴线(data_interval=Interval.MINUTE_5)
    )
)
```

### 回测执行

**2024-03-15 10:30 这根5分钟K线**：

1. **日线序列构造**：
   ```
   [
       3月1日完整日K,
       3月2日完整日K,
       ...
       3月14日完整日K,
       3月15日虚拟日K(close=10:30的5分钟收盘价)  # 实时合成
   ]
   ```

2. **日线条件评估**：
   - 用上述序列计算MA5/MA10/MA20
   - 判断多头排列：MA5 > MA10 > MA20
   - 判断回踩：price接近MA20

3. **分钟条件评估**：
   - 用5分钟K线序列
   - 判断缩量阴线

4. **信号触发**：两者都满足 → 记录买入信号

## 关键优势

### 1. 避免未来函数
- 日线条件不使用"未来"的完整日K线
- 虚拟当天日线只用到当前分钟的数据

### 2. 精确timing
- 主循环遍历5分钟K线
- 信号触发时刻精确到5分钟级别

### 3. 数据复用
- 日线数据在MTF Buffer中一次加载
- 每根分钟K线只需合成一次虚拟日K

## 文件修改清单

1. **vnpy/strategy_condition/engine/scan_engine.py** (line ~410-457)
   - 新增 `_build_realtime_daily_bars()` 方法
   - 在主循环中调用，构造实时日线序列

2. **vnpy/strategy_condition/ui/widget.py** (line ~1533-1567)
   - 在 `_on_backtest()` 中新增主循环数据切换逻辑
   - 多周期时加载分钟数据替换bars_dict

## 验证要点

1. 确认主循环遍历的是分钟数据
2. 确认日线条件查询到的是实时合成的日线序列
3. 确认虚拟当天日线的close等于当前分钟close
4. 确认信号触发时刻与分钟K线对应

## 使用说明

### 在UI中使用

1. 打开策略条件引擎
2. 创建混合周期策略：
   - 添加日线条件，设置`data_interval=日线`
   - 添加分钟条件，设置`data_interval=5分钟`
3. 选择股票池
4. 点击"回测验证"
5. 系统自动：
   - 识别混合周期
   - 加载日线数据到MTF Buffer
   - 加载5分钟数据作为主循环
   - 按分钟遍历并实时合成日线

### 代码示例

```python
from vnpy.strategy_condition.core import ConditionStrategy
from vnpy.strategy_condition.indicators import *
from vnpy.trader.constant import Interval

# 定义策略
strategy = ConditionStrategy(
    name="强势回踩",
    buy_tree=AND(
        # 日线：均线多头
        均线多头排列(data_interval=Interval.DAILY),
        # 日线：回踩MA20
        回踩MA20(data_interval=Interval.DAILY, tolerance=0.02),
        # 5分钟：缩量阴线
        缩量阴线(data_interval=Interval.MINUTE_5, vol_ratio=0.8)
    )
)

# 回测
from vnpy.strategy_condition.engine import ScanEngine
se = ScanEngine(ce)
results = se.backtest(
    symbols=['600000.SH', '000001.SZ'],
    strategy=strategy,
    bars_dict=minute_bars_dict,  # 5分钟数据
    warmup=60,
    is_intraday=True,
    execution_interval=Interval.MINUTE_5
)
```

## 性能优化

1. **日线数据预加载**：MTF Buffer一次性加载全部历史日线
2. **虚拟日K缓存**：同一天内重复使用已合成的虚拟K线
3. **条件级路由**：根据data_interval自动选择数据源

## 已完成状态

✅ scan_engine.py - 实时日线合成逻辑  
✅ widget.py - 主循环数据切换  
✅ mtf_auto_loader.py - 自动周期检测  
✅ condition_engine.py - MTF条件评估  
✅ 避免未来函数 - Phase 5 as_of_time对齐  

## 技术负责人

实现日期：2026-08-16  
版本：v1.0  
状态：已完成并测试通过

---

**总结**：现在系统完整支持混合周期策略。日线条件使用真实历史日线+虚拟当天日线（实时合成），分钟条件使用真实分钟数据，主循环按分钟遍历，完全避免未来函数。