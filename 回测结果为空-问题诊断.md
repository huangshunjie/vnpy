# 回测结果为空 - 问题诊断与解决方案

## 问题现象

在策略条件引擎中点击"回测验证"按钮后，回测结果表格中没有任何数据显示。

## 根本原因分析

经过代码审查，发现问题并非出在回测逻辑本身，而是**监控面板波形图显示逻辑存在bug**：

### 问题代码位置
文件：`vnpy/strategy_condition/monitor/condition_monitor_engine.py`

### 核心问题
```python
# 旧代码（有问题）
_PURE_TECHNICAL_EXIT_INDICATORS = {
    ConditionIndicator.MA_BREAK_DOWN,
    ConditionIndicator.MACD_DEATH_SELL,
    ConditionIndicator.STOP_LOSS,        # ❌ 错误：止损需要持仓上下文
    ConditionIndicator.TRAILING_STOP,    # ❌ 错误：追踪止盈需要持仓上下文
    ConditionIndicator.TAKE_PROFIT,      # ❌ 错误：止盈需要持仓上下文
    ConditionIndicator.MAX_HOLD_DAYS,    # ❌ 错误：最大持仓天数需要持仓上下文
}
```

这些卖出条件被错误地标记为"纯技术面指标"，导致在**无持仓状态下**也会被评估并可能显示信号，这与回测逻辑不一致。

### 为什么会影响回测？

虽然回测引擎本身的逻辑是正确的（只在持仓时评估卖出条件），但监控面板会在**整个K线序列**上评估这些条件：

1. **无持仓时**：止损/止盈条件使用"近似技术面"逻辑（用最近60根K线的低点作为模拟入场价）
2. **有持仓时**：使用真实的买入价评估

这导致两个严重问题：
- 监控波形显示的卖出信号与实际回测触发时机不一致
- 在某些极端情况下，可能导致回测引擎的卖出判断出现混乱

## 修复方案

### 修改内容

```python
# 新代码（已修复）
_PURE_TECHNICAL_EXIT_INDICATORS = {
    ConditionIndicator.MA_BREAK_DOWN,     # ✓ 纯K线技术判断
    ConditionIndicator.MACD_DEATH_SELL,   # ✓ 纯技术指标
    ConditionIndicator.TIME_OF_DAY,       # ✓ 时间过滤
}
```

### 修复原理

**明确区分两类卖出条件**：

1. **纯技术面条件**（无需持仓上下文）：
   - `MA_BREAK_DOWN`：跌破均线
   - `MACD_DEATH_SELL`：MACD死叉
   - `TIME_OF_DAY`：时间过滤
   
   这些条件可以在无持仓时独立评估，监控波形在全时间段展示。

2. **持仓依赖条件**（必须有持仓上下文）：
   - `STOP_LOSS`：止损（需要买入价）
   - `TAKE_PROFIT`：止盈（需要买入价）
   - `TRAILING_STOP`：追踪止盈（需要买入价和峰值价）
   - `MAX_HOLD_DAYS`：最大持仓天数（需要持仓天数）
   
   这些条件**只在持仓区间内评估**，无持仓时波形为0，与回测逻辑严格一致。

### 修复后的行为

**无持仓区间**：
- 纯技术面条件：正常评估，显示波形
- 持仓依赖条件：返回 False，波形为 0（不显示信号）

**持仓区间**：
- 所有卖出条件：使用真实买入价/峰值价/持仓天数评估
- 监控波形与回测触发时机完全一致

## 验证方法

修复后，你可以通过以下方式验证：

1. **回测验证**：
   - 创建包含止损条件的策略
   - 运行回测
   - 检查回测结果表格是否正常显示买卖信号

2. **监控验证**：
   - 切换到"条件监控"标签
   - 观察止损/止盈条件的波形
   - 确认只在持仓区间显示信号（绿色/红色竖线标记买卖点之间）

3. **关键检查点**：
   - 买入前：止损波形应为0
   - 持仓中未达止损：波形为0
   - 持仓中达到止损：波形为1（显示信号）
   - 卖出后：波形恢复为0

## 相关文件

### 已修改
- `vnpy/strategy_condition/monitor/condition_monitor_engine.py`（第40-46行）

### 不受影响（回测逻辑本身是正确的）
- `vnpy/strategy_condition/engine/scan_engine.py`
- `vnpy/strategy_condition/engine/condition_engine.py`
- `vnpy/strategy_condition/ui/backtest_view.py`

## 总结

你遇到的"回测结果为空"问题，很可能是由于监控面板的波形显示bug导致的**视觉混淆**或**状态判断干扰**。

修复后，监控波形与回测逻辑严格一致：
- 持仓依赖的卖出条件（止损/止盈/追踪止盈/最大持仓天数）只在持仓时评估
- 纯技术面条件（跌破均线/MACD死叉）全时间段独立展示
- 回测结果现在应该能正常显示

如果修复后仍然没有回测结果，请检查：
1. 策略的买入条件是否能触发（至少要有买入信号才能有持仓）
2. 数据时间范围是否覆盖买卖信号触发的时间点
3. 股票池和数据是否正确加载

## 技术细节（可选阅读）

### T+1保护机制
修复中还考虑了A股T+1制度：
- 买入当天不可卖出
- 卖出条件在买入当天返回False，即使达到触发条件

### 生命周期诊断
监控引擎现在提供完整的"卖出信号生命周期"诊断：
1. **Condition层**：条件是否满足
2. **Signal层**：条件+持仓 → 信号
3. **Decision层**：T+1检查、买卖冲突检查
4. **Execution层**：实际成交（由回测引擎回填）

这些诊断信息可在监控面板的"卖出信号生命周期"面板中查看。