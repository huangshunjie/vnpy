# VeighNa 数据库从 D 盘迁移到 E 盘 - 操作说明

## 📋 当前状态

✅ **已完成的步骤：**
1. ✅ 已创建 E 盘目标目录：`E:\vnpy_data\`
2. ✅ 已更新配置文件：`C:\Users\11229\.vntrader\vt_setting.json`
   - 旧路径：`D:\vnpy_data\database.db`
   - 新路径：`E:\vnpy_data\database.db`

⚠️ **待完成的步骤：**
- ⏳ 移动数据库文件（需要手动操作）

---

## 🚀 移动数据库文件的步骤

### 第 1 步：关闭所有 VeighNa 程序

**重要：** 必须先关闭所有正在运行的 VeighNa 相关程序，否则数据库文件被占用，无法移动。

需要关闭的程序包括：
- VeighNa Trader 主程序
- 策略条件引擎
- 量化研究平台
- K线行为实验室
- 任何其他使用数据库的 VeighNa 模块

**验证方法：**
- 打开任务管理器（Ctrl + Shift + Esc）
- 查找并关闭所有 `python.exe` 或 `pythonw.exe` 进程（与 VeighNa 相关的）

---

### 第 2 步：移动数据库文件

#### 方法一：使用文件资源管理器（推荐）

1. 打开文件资源管理器
2. 导航到 `D:\vnpy_data\`
3. 找到 `database.db` 文件（约 101 GB）
4. **右键点击** → 选择 **剪切**（或按 Ctrl + X）
5. 导航到 `E:\vnpy_data\`
6. **右键点击空白处** → 选择 **粘贴**（或按 Ctrl + V）
7. 等待文件移动完成（可能需要几分钟，取决于磁盘速度）

#### 方法二：使用命令行

打开命令提示符（以管理员身份运行），执行：

```cmd
move D:\vnpy_data\database.db E:\vnpy_data\database.db
```

---

### 第 3 步：验证迁移

移动完成后，验证文件是否成功迁移：

1. **检查新位置：**
   - 打开 `E:\vnpy_data\`
   - 确认 `database.db` 文件存在
   - 确认文件大小约为 101 GB

2. **检查旧位置：**
   - 打开 `D:\vnpy_data\`
   - 确认 `database.db` 文件**已不存在**

3. **运行验证脚本：**
```cmd
python -c "from pathlib import Path; db_path = Path('E:/vnpy_data/database.db'); print(f'数据库文件存在: {db_path.exists()}'); print(f'文件大小: {db_path.stat().st_size / (1024**3):.2f} GB' if db_path.exists() else '文件不存在')"
```

---

### 第 4 步：启动程序测试

1. 启动 VeighNa Trader 或相关程序
2. 观察程序是否正常启动
3. 检查数据是否正常加载
4. 确认没有数据库连接错误

---

## 🔧 配置文件详情

配置文件位置：`C:\Users\11229\.vntrader\vt_setting.json`

已修改的配置项：
```json
{
    "database.database": "E:\\vnpy_data\\database.db"
}
```

如果需要恢复到 D 盘，可以运行：
```python
python -c "import json; from pathlib import Path; config_path = Path.home() / '.vntrader' / 'vt_setting.json'; config = json.load(open(config_path, 'r', encoding='utf-8')); config['database.database'] = 'D:\\\\vnpy_data\\\\database.db'; json.dump(config, open(config_path, 'w', encoding='utf-8'), indent=4, ensure_ascii=False)"
```

---

## ⚠️ 故障排查

### 问题 1：无法移动文件，提示"文件正在使用"

**解决方案：**
1. 确保已关闭所有 VeighNa 程序
2. 使用任务管理器结束所有 Python 进程
3. 如果仍然无法移动，重启计算机后再尝试

### 问题 2：移动后程序无法启动

**解决方案：**
1. 检查 `E:\vnpy_data\database.db` 文件是否存在
2. 检查配置文件 `C:\Users\11229\.vntrader\vt_setting.json` 是否正确
3. 确认新路径拼写正确（注意反斜杠 `\\`）

### 问题 3：程序启动后提示找不到数据

**解决方案：**
1. 确认数据库文件完整性（大小约 101 GB）
2. 检查文件是否损坏（可以使用 SQLite 工具验证）
3. 如果文件损坏，从备份恢复或重新下载数据

---

## 📊 相关文件位置

| 类型 | 路径 |
|------|------|
| 数据库文件（旧） | `D:\vnpy_data\database.db` |
| 数据库文件（新） | `E:\vnpy_data\database.db` |
| 配置文件 | `C:\Users\11229\.vntrader\vt_setting.json` |
| VeighNa 主目录 | `C:\Users\11229` |
| 项目代码目录 | `c:\Users\11229\Documents\GitHub\vnpy` |

---

## 📝 备注

- 数据库文件大小：约 101 GB
- 迁移时间估计：5-15 分钟（取决于磁盘速度）
- 建议在非交易时间进行迁移
- 迁移前建议备份数据库文件（如果磁盘空间允许）

---

## ✅ 完成确认

完成所有步骤后，运行以下命令确认配置：

```cmd
python -c "from vnpy.trader.utility import get_file_path; from vnpy.trader.setting import SETTINGS; from pathlib import Path; db_path = get_file_path(SETTINGS.get('database.database', 'database.db')); print(f'配置的数据库路径: {db_path}'); print(f'文件是否存在: {Path(db_path).exists()}')"
```

预期输出：
```
配置的数据库路径: E:\vnpy_data\database.db
文件是否存在: True
```

---

**创建时间：** 2026-08-14  
**脚本位置：** `update_database_path.py`（用于更新配置）