# 多周期回测执行粒度优化 - 完成报告

## 问题描述

用户场景：
- **买入条件**：均线多头排列 + 回踩MA20（数据周期=日线）
- **卖出条件**：缩量阴线（数据周期=5分钟）

**期望行为**：
- 主循环按5分钟粒度遍历
- 每根5分钟K线都评估买卖条件
- 日线条件用当前分钟close作为虚拟日线close评估

**原有问题**：
- `execution_interval` 始终等于 `anchor_interval`（用户设置的K线周期=日线）
- 主循环按日线推进，无法捕捉分钟级的卖出信号

---

## 解决方案

### 核心改动：mtf_auto_loader.py

修改 `analyze_strategy_data_requirements()` 函数，使 `execution_interval` 按以下规则确定：

```python
if len(all_intervals) == 1:
    # 单周期策略：execution_interval = 该周期
    execution_interval = all_intervals[0]
else:
    # 多周期策略：execution_interval = 最小周期（最高粒度）
    interval_priority = {
        Interval.MINUTE: 1,
        Interval.MINUTE_5: 5,
        Interval.MINUTE_15: 15,
        Interval.MINUTE_30: 30,
        Interval.HOUR: 60,
        Interval.DAILY: 1440,
        Interval.WEEKLY: 10080,
    }
    min_interval = min(all_intervals, key=lambda i: interval_priority.get(i, 999999))
    execution_interval = min_interval
```

### 示例场景

| 买入条件周期 | 卖出条件周期 | execution_interval | 主循环步长 |
|------------|------------|-------------------|----------|
| 日线 | 日线 | 日线 | 每根日线 |
| 日线 | 5分钟 | **5分钟** | **每根5分钟** |
| 5分钟 | 5分钟 | 5分钟 | 每根5分钟 |
| 15分钟 | 5分钟 | 5分钟 | 每根5分钟 |

---

## 实现细节

### 1. mtf_auto_loader.py（第67-95行）

```python
# 确定 execution_interval（主循环步长）
all_intervals = list(combined_req.intervals)

if len(all_intervals) == 1:
    execution_interval = all_intervals[0]
else:
    # 找到数值最小的周期（粒度最细）
    interval_priority = {...}
    min_interval = min(all_intervals, key=lambda i: interval_priority.get(i, 999999))
    execution_interval = min_interval

combined_req.strategy_execution_interval = execution_interval
combined_req.execution_interval = execution_interval
```

### 2. scan_engine.py（第459-608行）

主循环逻辑已就绪：

```python
i = warmup
while i < n_bars - 1:  # 按 execution_interval 的粒度遍历
    bars_so_far = all_bars[:i + 1]
    
    # 如果是多周期 + 分钟执行 + 需要日线条件
    if needs_realtime_daily and interval == Interval.DAILY:
        # 使用实时合成的日线序列（第388-457行）
        realtime_daily = _build_realtime_daily_bars(i)
        ctx.set_bars(interval, realtime_daily)
    
    # 评估买入条件
    passed, score = strategy.buy_tree.evaluate(...)
    
    # 评估卖出条件（同理）
    ...
```

### 3. 实时日线合成机制（第360-457行）

当执行周期是分钟线且存在日线条件时，使用分钟close实时合成虚拟日线：

```python
def _build_realtime_daily_bars(cur_bar_idx: int) -> list:
    """
    构造实时日线序列 = 历史完整日线（截至昨天） + 虚拟当天日线
    虚拟当天日线的close使用当前分钟K线的close
    """
    # 检测新的一天 → 重置统计
    if cur_date != _rt_prev_date:
        _rt_today_open = cur_bar.open
        _rt_today_high = cur_bar.high
        _rt_today_low = cur_bar.low
        _rt_today_volume = float(cur_bar.volume)
    else:
        # 同一天，累积统计
        if cur_bar.high > _rt_today_high:
            _rt_today_high = cur_bar.high
        if cur_bar.low < _rt_today_low:
            _rt_today_low = cur_bar.low
        _rt_today_volume += float(cur_bar.volume)
    
    # 构造虚拟当天日线 BarData
    virtual_bar = _BarData(
        open=_rt_today_open,
        high=_rt_today_high,
        low=_rt_today_low,
        close=cur_bar.close,  # ← 使用当前分钟close
        volume=_rt_today_volume,
        ...
    )
    
    return historical_daily_bars[:cutoff_idx] + [virtual_bar]
```

---

## 回测流程示例

### 场景：日线买入 + 5分钟卖出

1. **数据加载**（widget.py 第1530行 + mtf_auto_loader.py）
   - UI设置：K线周期=日线，K线数量=300
   - 策略分析：需要日线（买入）+ 5分钟（卖出）
   - **execution_interval = 5分钟**（最小周期）
   - 加载：日线300根 + 对应时间范围的5分钟数据

2. **主循环**（scan_engine.py 第459行）
   ```python
   i = warmup
   while i < n_bars - 1:  # n_bars = 5分钟K线总数
       # 每次 i += 1 = 前进一根5分钟K线
   ```

3. **日线条件评估**（scan_engine.py 第477-481行）
   - 检测到日线条件 → 调用 `_build_realtime_daily_bars(i)`
   - 用第i根5分钟K线的close作为虚拟日线close
   - 评估"均线多头排列 + 回踩MA20"

4. **5分钟条件评估**（scan_engine.py 第569-582行）
   - 直接使用第i根5分钟K线
   - 评估"缩量阴线"

5. **买入信号生成**（第527-539行）
   - 如果买卖条件同时满足 → 触发买入

6. **持仓期卖出检查**（第562-582行）
   - 每根5分钟K线都检查"缩量阴线"
   - 满足 → 平仓，记录卖出信号

---

## 关键特性

✅ **自动粒度适配**
- 单周期策略：主循环=该周期（向后兼容）
- 多周期策略：主循环=最小周期（最高粒度）

✅ **实时日线合成**
- 分钟级回测中，用当前分钟close实时合成虚拟日线
- 避免未来函数（只用历史完整日线 + 当天虚拟日线）

✅ **条件级路由**（scan_engine.py 第495-502行）
- 单周期条件：使用普通 `eval_condition()`
- 多周期条件：使用 `eval_condition_mtf()` + MTFContext

✅ **性能优化**
- 预计算 numpy 数组（scan_engine.py 第330-346行）
- numpy切片 = O(1) view，避免重复数组提取

---

## 向后兼容

| 场景 | execution_interval | 行为 |
|------|-------------------|------|
| 纯日线策略 | 日线 | 按日线遍历（不变） |
| 纯5分钟策略 | 5分钟 | 按5分钟遍历（不变） |
| 日线+5分钟混合 | 5分钟 | **新行为**：按5分钟遍历 |

---

## 测试建议

### 单元测试
```python
def test_execution_interval_selection():
    # 单周期策略
    req = analyze_strategy_data_requirements(daily_only_strategy, Interval.DAILY, 300)
    assert req.execution_interval == Interval.DAILY
    
    # 多周期策略（日线 + 5分钟）
    req = analyze_strategy_data_requirements(mixed_strategy, Interval.DAILY, 300)
    assert req.execution_interval == Interval.MINUTE_5  # 最小周期
    
    # 多周期策略（15分钟 + 5分钟）
    req = analyze_strategy_data_requirements(intraday_strategy, Interval.MINUTE_15, 300)
    assert req.execution_interval == Interval.MINUTE_5
```

### 端到端测试
```python
def test_mixed_timeframe_backtest():
    # 策略：日线买入 + 5分钟卖出
    strategy = Strategy(
        buy_tree=ConditionNode(indicator=MA_ALIGNMENT, data_interval=Interval.DAILY),
        sell_tree=ConditionNode(indicator=KLINE_SHRINK_YIN, data_interval=Interval.MINUTE_5)
    )
    
    # 回测
    batch = scan_engine.backtest(
        symbols=["600519.SSE"],
        strategy=strategy,
        all_bars_dict={
            "600519.SSE": minute_5_bars  # 5分钟数据
        },
        execution_interval=Interval.MINUTE_5  # 由 mtf_auto_loader 自动确定
    )
    
    # 验证：应该能捕捉到5分钟级别的卖出信号
    assert batch.count > 0
    assert any(s.exit_reason == "kline_shrink_yin" for s in batch.signals)
```

---

## 文件清单

| 文件 | 修改内容 |
|------|---------|
| `vnpy/strategy_condition/core/mtf_auto_loader.py` | 第67-95行：execution_interval按最小周期确定 |
| `vnpy/strategy_condition/engine/scan_engine.py` | 第360-608行：实时日线合成 + 主循环逻辑（已就绪，无需修改） |
| `vnpy/strategy_condition/ui/widget.py` | 第1530行：使用 mtf_auto_loader 分析策略（已集成，无需修改） |

---

## 完成状态

✅ **核心逻辑**：mtf_auto_loader.py execution_interval按最小周期确定  
✅ **回测引擎**：scan_engine.py 主循环 + 实时日线合成（已就绪）  
✅ **UI集成**：widget.py 使用 mtf_auto_loader（已集成）  
✅ **向后兼容**：单周期策略不受影响  

---

## 使用说明

### 1. 创建混合周期策略

```python
from vnpy.strategy_condition.core.strategy import Strategy
from vnpy.strategy_condition.core.condition import Condition
from vnpy.strategy_condition.constant import ConditionIndicator
from vnpy.trader.constant import Interval

# 买入条件：日线均线多头排列
buy_cond = Condition(
    indicator=ConditionIndicator.MA_ALIGNMENT,
    data_interval=Interval.DAILY,  # ← 指定数据周期
    params={...}
)

# 卖出条件：5分钟缩量阴线
sell_cond = Condition(
    indicator=ConditionIndicator.KLINE_SHRINK_YIN,
    data_interval=Interval.MINUTE_5,  # ← 指定数据周期
    params={...}
)

strategy = Strategy(
    name="日线买入5分钟卖出",
    buy_tree=ConditionNode(op=NodeOp.AND, children=[buy_cond]),
    sell_tree=ConditionNode(op=NodeOp.OR, children=[sell_cond])
)
```

### 2. 执行回测

在UI中：
1. K线周期选择：**日线**（用于确定数据范围，加载300根日线）
2. K线数量：300
3. 系统自动：
   - 检测策略需要日线+5分钟
   - 加载对应时间范围的5分钟数据
   - **主循环按5分钟粒度遍历**
   - 日线条件用分钟close实时合成评估

### 3. 查看结果

- 回测结果Tab：显示所有交易信号（按5分钟粒度生成）
- K线图Tab：可切换查看日线/5分钟图
- 条件监控Tab：查看每根K线的条件评估详情

---

## 总结

通过修改 `mtf_auto_loader.py` 的 `execution_interval` 确定逻辑，系统现在能够：

1. **自动识别**策略的最小数据周期
2. **按最小周期粒度**执行回测主循环
3. **实时合成**更大周期的虚拟K线（用当前小周期close）
4. **完全向后兼容**单周期策略

用户的场景（日线买入 + 5分钟卖出）现在可以正确回测，主循环会遍历每根5分钟K线，捕捉所有分钟级的卖出信号。