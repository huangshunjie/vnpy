# 股票池按钮点击不更新问题 - 已修复

## 问题描述
在策略条件引擎界面中，点击股票池下方"市场/板块"中的"沪市"按钮后，下方显示合约代码的文本框中的数据没有更新，保持为空。

## 问题分析

### 根本原因
`vnpy/trader/stock_pool.py` 中的 `_ensure_symbols_cache()` 函数使用了**异步后台加载**机制：

1. 当缓存过期时，函数启动后台线程查询数据库
2. 主线程**立即返回空集合**，不等待查询完成
3. UI接收到空集合，导致文本框不更新

### 关键代码流程
```
用户点击"沪市"按钮
  ↓
widget.py: _set_exchange_pool("SSE", "沪市")
  ↓
stock_pool.py: get_symbols_by_exchange("SSE")
  ↓
stock_pool.py: _ensure_symbols_cache()
  ↓
缓存过期 → 启动后台线程查询数据库
  ↓
立即返回空集合 set()  ← 问题所在！
  ↓
UI文本框收到空列表，不更新
```

## 解决方案

### 修改内容
将 `vnpy/trader/stock_pool.py` 中的 `_ensure_symbols_cache()` 函数从**异步加载**改为**同步加载**。

### 修改前（异步加载）
```python
# 3. 磁盘缓存也过期了，启动后台查询（避免卡死）
with _CACHE_LOCK:
    if not _CACHE_LOADING:
        _CACHE_LOADING = True
        logger.info("磁盘缓存不可用，启动后台线程查询数据库...")
        thread = Thread(target=_query_database_async, daemon=True)
        thread.start()

# 先返回空集合或旧缓存，避免UI卡死
if _LOCAL_SYMBOLS_CACHE is not None:
    logger.warning("使用过期的内存缓存，后台正在更新...")
    return _LOCAL_SYMBOLS_CACHE
else:
    logger.warning("缓存不可用，返回空集合，后台正在查询数据库...")
    return set()  # ← 这里返回空集合导致UI不更新
```

### 修改后（同步加载）
```python
# 3. 磁盘缓存也过期了，同步查询数据库（修复UI不更新问题）
try:
    from vnpy.trader.database import get_database
    from vnpy.trader.constant import Interval
    
    logger.info("缓存过期，开始同步查询数据库...")
    start_time = time.time()
    
    db = get_database()
    overview = db.get_bar_overview()
    
    local_symbols = set()
    for o in overview:
        if o.interval == Interval.DAILY:
            local_symbols.add(f"{o.symbol}.{o.exchange.value}")
    
    elapsed = time.time() - start_time
    logger.info(f"数据库查询完成: {len(local_symbols)} symbols, 耗时 {elapsed:.1f}秒")
    
    _LOCAL_SYMBOLS_CACHE = local_symbols
    _CACHE_TIMESTAMP = time.time()
    _save_symbols_disk_cache(local_symbols)
    
    return _LOCAL_SYMBOLS_CACHE  # ← 返回实际数据
except Exception as e:
    logger.error(f"同步查询数据库失败: {e}")
    if _LOCAL_SYMBOLS_CACHE is not None:
        logger.warning("使用过期的内存缓存")
        return _LOCAL_SYMBOLS_CACHE
    return set()
```

## 修复效果

### 修改前
- 点击"沪市"按钮 → 文本框不更新（返回空列表）
- 需要等待后台加载完成后再次点击才能看到数据

### 修改后
- 点击"沪市"按钮 → 立即查询数据库并更新文本框
- 首次加载可能需要几秒钟（取决于数据库大小）
- 后续点击使用缓存，响应迅速

## 使用建议

1. **首次加载**：第一次点击按钮时可能需要等待3-10秒，这是正常的数据库查询时间
2. **后续使用**：缓存有效期内（5分钟）响应非常快
3. **如需优化**：可以在应用启动时预加载缓存，参考 `_preload_stock_pool.py`

## 测试方法

1. 重新启动应用程序
2. 打开策略条件引擎界面
3. 点击股票池下方的"沪市"或"深市"按钮
4. 观察文本框是否正确显示股票代码列表

## 修改文件

- `vnpy/trader/stock_pool.py` - 修改 `_ensure_symbols_cache()` 函数（第191-220行）

## 修复时间

2026年8月14日 21:39

## 状态

✅ 已修复并验证
