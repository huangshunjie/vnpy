# 日线-分钟K线联动功能说明

## 功能概述
在 Monitor 标签页的双周期或分钟模式下，点击日线K线可以自动定位到对应日期的分钟K线，并显示该日期的买卖信号。

## 使用步骤

### 1. 切换到Monitor Tab
在主界面点击"条件监控 Monitor"标签页

### 2. 选择显示模式
在顶部工具栏选择：
- "双周期"模式：同时显示日线和分钟K线
- "分钟"模式：只显示分钟K线（但日线点击仍可联动）

### 3. 加载数据
确保已经加载了股票的监控数据（日线和分钟数据都有）

### 4. 点击日线K线
直接用鼠标左键点击日线K线图中的任意一根K线

### 5. 查看联动效果
- 分钟K线图会自动切换到被点击的日线对应的日期
- 如果该日期有买入或卖出信号，会在分钟K线中显示
- 标题会显示："股票代码 分钟触发/分钟退出 - 日期 (X bars)"

## 技术实现

### 核心组件
1. **KlineChartWidget.bar_clicked 信号**
   - 点击K线时发射，携带被点击bar的datetime对象

2. **ConditionMonitorWidget._on_daily_bar_clicked() 方法**
   - 接收日线点击事件
   - 提取该日期的买卖信号
   - 调用分钟面板的focus_date方法

3. **_PeriodMonitorPanel.focus_date() 方法**
   - 筛选指定日期的分钟K线数据
   - 筛选对应的snapshots
   - 重新加载K线图、波形图和信号标记

### 信号连接时机
在首次加载日线数据时（load_layered_data方法中），延迟连接点击信号，确保chart已完全初始化。

## 故障排查

如果点击日线没有反应：

1. **检查模式**：确保不在"日线"单独模式（该模式不支持联动）
2. **检查数据**：确保已加载分钟数据
3. **重新加载**：点击"应用策略"按钮重新加载监控数据
4. **查看日志**：控制台会输出信号连接和处理的调试信息

## 扩展功能

### 全屏模式
点击全屏按钮后，全屏K线图同样支持点击事件（预留接口，可扩展实现更多联动功能）

### 波形区联动
分钟K线切换后，条件波形图也会同步显示对应日期的条件满足情况

## 注意事项

1. 只在有分钟数据的日期才会联动成功
2. 信号匹配基于datetime精确对比，确保数据时间戳准确
3. 点击同一日期多次会重新加载，无缓存

## 未来优化方向

1. 添加点击反馈动画
2. 支持键盘快捷键切换日期
3. 添加日期范围选择器
4. 支持分钟K线点击联动到日线

---
实现日期：2026-08-20
版本：v1.0